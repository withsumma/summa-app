# Dependencies (installed by npm, don't commit these — they're huge)
node_modules

# Environment variables (contains your secret keys — NEVER commit this)
.env
.env.local

# Build output (Vite generates this when you deploy — Vercel builds it for you)
dist

# Editor and OS files
.DS_Store
*.swp
